import { Fragment, VoidFunctionComponent } from "react";

export const App: VoidFunctionComponent = () => {
    return (
        <Fragment>
            <h4>App Component</h4>
        </Fragment>
    );
}